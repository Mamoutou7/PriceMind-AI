{
  "mcpServers": {
    "scraping_mcp": {
      "command": "uv",
      "args": ["run", "python", "-m", "apps.scraping_mcp.server"]
    },
    "parsing_mcp": {
      "command": "uv",
      "args": ["run", "python", "-m", "apps.parsing_mcp.server"]
    },
    "storage_mcp": {
      "command": "uv",
      "args": ["run", "python", "-m", "apps.storage_mcp.server"]
    }
  }
}